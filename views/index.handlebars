<div class="container">
    <h1 class="display-4">Bem-vindo ao blog do rafu</h1>
    <p class="lead">Isto é apenas uma demonstração</p>
    <hr class="my-4">
    <p>Isto utiliza as seguintes tecnologias: express, handlebars, mongodb e node.js</p>
    <p class="lead">
        <a class="btn btn-primary btn-lg" href="#" role="button">Crie uma conta</a>
    </p>
</div>
<h2>Postagens recentes</h2>

{{#each posts}}
<div class="card">
    <div class="card-body">
        <h3>{{title}}</h3>
        <p>{{description}}</p>
        <a href="/posts/{{slug}}"><button class="btn btn-success">Leia mais</button></a>
        <hr>
        <small>Data de publicação: {{date}}</small>
    </div>
</div>
{{else}}
<h5>Nenhum postagem</h5>
{{/each}}